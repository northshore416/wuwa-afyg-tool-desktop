# WW Combo Trainer v0.3.0

发布日期：2026-07-30

本版本集中完善了内置教程、输入与快捷键、时间轴编辑、外观、视频工具和 Windows 便携发布流程。桌面程序支持中文、English、日本語、한국어；本项目为非官方粉丝工具，不会向游戏发送按键。

## 下载与安装

- `WW Combo Trainer_0.3.0_x64-setup.exe`：推荐的 Windows 安装程序。
- `WW Combo Trainer_0.3.0_x64_en-US.msi`：Windows MSI 安装包。
- `wwcombo-正式版-v0.3.0-Windows-x64.zip`：便携正式版。解压完整目录后，通过外层快捷方式启动；`便携版` 文件夹内已包含本体和 FFmpeg。
- `SHA256SUMS-v0.3.0.txt`：下载文件的 SHA-256 校验值。

程序尚未购买代码签名证书，Windows 可能显示未知发布者提示。请只从本仓库 Release 下载。桌面版会在启动时请求管理员权限；确认 UAC 后，全局输入捕获才能可靠读取与游戏同权限级别的键鼠输入。

## 帮助与语言

- 新增中文、英语、日语、韩语内置帮助，包含“练习者”“攻略作者”“详细”“更新日志”分类和首次离开主界面时的阅读引导。
- 四语言 README 按实际工作流说明 JSON 导入、键位设置、全局捕获、三种练习模式、录制编辑、视频工具、轴图导出和按键映射。
- 设置支持导入、导出独立的 `.wwkeys.json` 按键配置；该格式与连段分享 JSON 明确区分。

## 输入与快捷键

- Windows EXE 强制请求管理员权限；完善全局捕获、鼠标侧键、每招式双键绑定和手柄输入链路。
- 设置新增“快捷键设置”，可修改单键和 Shift 组合；Ctrl、Alt 起手的固定编辑组合保持不变。
- 修复 `Delete`、`Ctrl+C`、`Ctrl+V`；有选择时 Delete 删除所选，无选择时进入连续删除模式。
- 默认 `C` 进入分割，`V` 合并选中块；屏蔽空格翻页等会干扰映射与编辑的浏览器默认操作。
- 添加模式新增 `F` 处决块和 `B` 变奏自适应切人；选中块后按 `Y` 可在内容后追加延奏 `y`。
- 补齐处决、变奏、延奏、前走的默认提示文本，并完善键鼠实际键帽、方形长按和手柄长按显示。

## 时间轴与录制

- 修复招式块拖动，以及 `Alt` + 拖动调整预输入和后摇失效的问题。
- 精简右键菜单，新增“更改”和“合并”；空白处右键也能对当前选择执行操作。
- 多个同角色、同类型、同轨道块可合并为一个连续大块。
- 录制状态提示点在等待时显示绿色、录制时显示红色。
- 录制时间轴新增播放、倍速与自动跟随，缩放以白色播放进度线为中心。
- 优化视频裁剪流程和工具栏：裁剪位于最左侧，五个编辑工具居中，多功能时间轴工具位于最右侧。

## 内容与外观

- 招式内容支持中括号纯文字，例如 `[Basic Attack]` 显示为 `Basic Attack`，且括号内文字不参与图标转换。
- 外观设置新增招式文字描边开关、粗细和颜色。
- 底图新增边缘保护参数，减少两端装饰被拉伸，并优化默认裁剪。
- 新增队伍预设；继续完善三套主题、主界面背景与主题图标、Live2D 展示及关闭 Live2D 的选项。
- 修正浅色主题中招式文字、括号和连段列表卡片的对比度。
- 移除主界面入口收起眼睛按钮的色块背景，并修复点击后跳位。

## 导出与实验工具

- 完善整张轴图 PNG 导出：支持启动轴、循环轴、轮次选择、自动换行缩放，以及模块独立的底图、裁剪、图标和映射。
- 完善视频导出、透明图层、按键映射与节奏合轴工具。
- 便携版现在可从本体同目录或 `resources` 子目录发现 FFmpeg，便携目录无需额外配置。

完整使用说明请查看压缩包内四语言 README，或在程序的“帮助”页中阅读。

## 2026-07-30 热修

- 重击的键鼠长按判定调整为 200 ms；其他键鼠长按为 300 ms，手柄长按单独调整为 500 ms。
- Windows 全局捕获新增原生 XInput 后台轮询，支持标准 XInput 手柄在游戏窗口前台时继续向录制、练习和按键映射发送输入。
- 练习、外观和按键映射页面的悬浮显示按钮由眼睛图标改为“悬浮”文字；主界面眼睛按钮保持不变。

## 2026-07-30 紧急修复

- 修复录制模式中带有长按版本的普通招式没有先创建原始操作块，导致除切人外的手柄操作无法录制的问题。
- 短按现在会在按下时立即记录；持续按住达到阈值后，再将已记录的原始块转换为对应长按块。
- 所有点按操作改为 0.5 秒滑动合并窗口：同一角色连续执行同一操作时，每次点按都会刷新等待时间，只有超过 0.5 秒没有继续点按才会拆成新的招式块；转成长按时保留的普通前段也会参与合并，长按段仍保持独立。
- 修复视频裁剪保存后主预览仍停留在原播放位置、看不出裁剪结果的问题；保存后主播放器会暂停并跳到裁剪起点，同时显示裁后时长并限制播放区间。
- 修复视频工具中本地视频偶发无法开始播放的问题；主播放器现在会主动加载媒体、等待可播放状态并显示具体读取或解码错误，重新导入视频时也不会继承上一段视频的裁剪位置。

## 2026-07-31 开发版更新

- “悬浮”改名为“连段图置顶”，改为黄色背景、黑色文字和眼睛图标；移动按钮改为十字移动图标。修复覆盖层跨越屏幕上下半区、提示箭头自动换边时，底图下缘被窗口裁剪的问题。全局捕获未开启时，左侧按钮上方会显示持续轻微移动的黄色指示三角。
- 设置新增全局自定义图标，可逐个上传、恢复，并覆盖键鼠、手柄和外观中的同名图标；自定义图标会随 `.wwkeys.json` 按键设置一起导入导出。
- 录制新增切人保护：当前已是目标角色时，不再记录重复切向同一角色的无效切人块。
- 录制页新增“文字轴识别”，支持角色中文名或首字简称、字母图标规则、大写长按、中文跳跃/闪避/处决等动作、变奏/延奏、启动轴和循环轴；默认时长为解放 3 秒、切人 0.5 秒、其他动作 1 秒。
- `C` 在选择块时会按当前鼠标时间直接分割所选块；无选择时仍进入连续分割模式。多选组保持到点击组外块，双击组内块可收束为单选。
- 普通时间轴与视频时间轴共享复制缓存，修复切换界面或重新挂载后 `Ctrl+C` / `Ctrl+V` 偶发失效。
- 首次使用弹窗新增四语言免费防骗提示：软件完全免费，任何平台付费购买都应立即申请退款。
